---
'@romanmartinidev/components': minor
'@romanmartinidev/tokens': minor
---

`DsSkeleton` (HU-010): placeholder de carga con shapes `text/rect/circle` (defaults tokenizados), `width`/`height`/`radius` CSS libres, decorativo siempre (`aria-hidden`) y pulso de opacidad que se apaga bajo `prefers-reduced-motion`. Cierra la tanda 1 de D-009.

Tokens: `component.skeleton.*` nuevos (bg `bg.disabled` theme-aware, radius, alturas por shape, duración y opacidad del pulso) — sin pares de contraste.
