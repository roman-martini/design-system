---
'@romanmartinidev/tokens': minor
---

feat: formalize z-index hierarchy in design-tokens-package spec + add semantic motion tokens for overlays (`transition.overlay-enter`, `transition.overlay-exit`) + add semantic effect tokens (`effect.blur.overlay`) preparing the system for Modal/Toast/Tooltip components. Adds Vitest tests validating the contract.
