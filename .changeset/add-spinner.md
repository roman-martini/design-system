---
'@romanmartinidev/components': minor
'@romanmartinidev/tokens': minor
---

`DsSpinner` (HU-009): indicador de espera indeterminada con sizes `xs/sm/md/lg` por tokens, color heredado por `currentColor` (sin API de color), anuncio `role="status"` con label opt-out (`label=""` → decorativo) y `prefers-reduced-motion` que reemplaza la rotación por un pulso de opacidad.

Tokens: `component.spinner.*` nuevos (sizes, stroke, track-opacity, duraciones de spin y pulso) — sin cambios de color ni pares de contraste.
