---
'@romanmartinidev/tokens': minor
---

feat(tooltip): new `component.tooltip.*` tokens — inverse theme-aware surface by pairing semantics (`bg` ← `text.primary`, `text` ← `bg.surface`, ~17:1 contrast in all 4 themes, verified by script), `delay` ← `motion.duration.slower` (500ms), plus padding, radius, font-size, max-width, shadow and offset.
