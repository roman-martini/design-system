# Generados por el bot de changesets en cada release (no pasan por hooks)
CHANGELOG.md

# Dependencies y build outputs
node_modules/
dist/
build/
out/
coverage/
.angular/
.changeset/
storybook-static/
documentation/
pnpm-lock.yaml
*.tsbuildinfo

# Scaffold importado (no editar manualmente: se sincroniza desde upstream)
.claude/

# Material histórico / de referencia (no normativo)
docs/reference/

# Packages que se migran/refactorizan en fases posteriores del bootstrap
# (formato se aplica en su fase, no en Fase 1)
packages/tokens/src/
packages/tokens/sd.config.mjs
