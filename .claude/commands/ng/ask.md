Usá el sub-agent `ng-router` para clasificar el pedido (crear / revisar / proponer cambio / sincronizar) y derivar a la pieza correcta (ng-component, ng-review o ng-sync como sub-agents; ng-change como skill), o resolverlo si es trivial.

$ARGUMENTS
